import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PreguntaImagenPage } from './pregunta-imagen';

@NgModule({
  declarations: [
    PreguntaImagenPage,
  ],
  imports: [
    IonicPageModule.forChild(PreguntaImagenPage),
  ],
})
export class PreguntaImagenPageModule {}
